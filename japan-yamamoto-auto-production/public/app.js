const DEMO_IMAGE="/assets/kubota-kh012.jpg";
async function getJSON(url, options={}){const r=await fetch(url,options);const data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||"Request failed");return data;}
function money(p){return p.priceMode==="show"&&p.price?p.price:"価格はお問い合わせください"}
function escapeHtml(s){return String(s??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#039;"}[c]));}
function card(p){const img=(p.images&&p.images[0])||DEMO_IMAGE;const chips=(p.specs||[]).slice(0,3).map(s=>`<span class="chip">${escapeHtml(s.key)}: ${escapeHtml(s.value)}</span>`).join("");const sold=p.status==="sold";return `<a class="product-card ${sold?"sold-card":""}" href="/product.html?id=${encodeURIComponent(p.id)}"><div class="product-image-wrap"><img class="product-img" src="${escapeHtml(img)}" onerror="this.onerror=null;this.src='${DEMO_IMAGE}'" alt="${escapeHtml(p.title)}">${sold?'<span class="sold-ribbon">売約済み</span>':''}</div><div class="product-body"><div class="product-meta">${escapeHtml(p.category||"機械")}</div><div class="product-title">${escapeHtml(p.title)}</div><div class="spec-chips">${chips}</div><strong>${escapeHtml(sold?"売約済み":money(p))}</strong></div></a>`;}
async function loadFeatured(){const root=document.querySelector("#featured");if(!root)return;try{const ps=await getJSON("/api/products");root.innerHTML=ps.filter(p=>p.status!=="sold").slice(0,3).map(card).join("")||"<p>機械はまだ登録されていません。</p>";}catch{root.innerHTML="<p>機械情報を読み込めませんでした。</p>";}}
let allProducts=[];
function sortProducts(ps){return [...ps].sort((a,b)=>{const rank={published:0,draft:1,sold:2};return (rank[a.status]??9)-(rank[b.status]??9);});}
async function loadProductsPage(){const root=document.querySelector("#products");if(!root)return;try{allProducts=sortProducts(await getJSON("/api/products"));renderProducts(allProducts);}catch{root.innerHTML="<p>機械情報を読み込めませんでした。</p>";}}
function renderProducts(ps){const root=document.querySelector("#products");if(!root)return;const active=sortProducts(ps.filter(p=>p.status==="published"));const sold=sortProducts(ps.filter(p=>p.status==="sold"));const activeHtml=active.length?`<section class="catalog-group"><div class="catalog-group-head"><div><div class="eyebrow">販売中</div><h2>販売中の機械</h2></div><span class="catalog-count">${active.length}台</span></div><div class="products-grid">${active.map(card).join("")}</div></section>`:`<section class="catalog-group"><div class="catalog-group-head"><div><div class="eyebrow">販売中</div><h2>販売中の機械</h2></div></div><div class="notice">現在、販売中の機械はありません。</div></section>`;const soldHtml=sold.length?`<section class="catalog-group sold-group"><div class="catalog-group-head"><div><div class="eyebrow">販売終了</div><h2>売約済みの機械</h2></div><span class="catalog-count">${sold.length}台</span></div><div class="products-grid">${sold.map(card).join("")}</div></section>`:"";root.innerHTML=activeHtml+soldHtml;}
function filterProducts(){const q=(document.querySelector("#search")?.value||"").toLowerCase();renderProducts(sortProducts(allProducts.filter(p=>JSON.stringify(p).toLowerCase().includes(q))));}
function ytId(url){try{const u=new URL(url);if(u.hostname==="youtu.be")return u.pathname.slice(1);if(u.hostname.includes("youtube.com"))return u.searchParams.get("v")||u.pathname.split("/").pop();}catch{}return "";}
function lineMailUrl(product){const message=`「${product?.title||"商品"}」についてメールを送ります。`;return `https://line.me/R/oaMessage/${encodeURIComponent("@yamamotoauto")}/?${encodeURIComponent(message)}`;}
async function loadProductDetail(){const id=new URLSearchParams(location.search).get("id"),el=document.querySelector("#detail");if(!el)return;if(!id){el.innerHTML='<div class="container section"><h2>機械が指定されていません</h2><a class="btn" href="/products.html">機械一覧に戻る</a></div>';return;}try{const p=await getJSON("/api/products/"+encodeURIComponent(id));document.title=p.title+" | 株式会社 山本オート";const main=(p.images&&p.images[0])||DEMO_IMAGE;const thumbs=(p.images||[]).map((im,i)=>`<img class="${i===0?"active":""}" src="${escapeHtml(im)}" onerror="this.onerror=null;this.src='${DEMO_IMAGE}'" onclick="document.querySelector('#mainImg').src=this.src;document.querySelectorAll('.thumbs img').forEach(x=>x.classList.remove('active'));this.classList.add('active')" alt="">`).join("");const rows=(p.specs||[]).map(s=>`<div class="spec-row"><b>${escapeHtml(s.key)}</b><span>${escapeHtml(s.value)}</span></div>`).join("");const video=ytId(p.videoUrl)?`<div class="video"><iframe src="https://www.youtube.com/embed/${encodeURIComponent(ytId(p.videoUrl))}" title="機械動画" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" allowfullscreen></iframe></div>`:"<div class='notice'>動画はまだ登録されていません。</div>";const sold=p.status==="sold";const contactBox=sold?`<div class="contact-box sold-contact"><h3>この機械は売約済みです</h3><p class="muted">現在、こちらの機械は販売を終了しています。</p></div>`:`<div class="contact-box"><h3>この機械について</h3><p class="muted">気になる点や購入について、LINEからメールを送ってご連絡いただけます。</p><a class="btn line" href="${lineMailUrl(p)}" target="_blank" rel="noopener">LINEからメールを送る</a><div style="margin-top:15px"><img class="qr" src="/assets/line-qr.png" onerror="this.style.display='none'" alt="LINE QR code"></div></div>`;el.innerHTML=`<section class="detail"><div class="container"><a class="muted" href="/products.html">← すべての機械</a><div class="detail-grid" style="margin-top:22px"><div><img id="mainImg" class="gallery-main" src="${escapeHtml(main)}" onerror="this.onerror=null;this.src='${DEMO_IMAGE}'" alt="${escapeHtml(p.title)}"><div class="thumbs">${thumbs}</div></div><div><div class="eyebrow">${escapeHtml(p.category||"機械")}</div><h1>${escapeHtml(p.title)}</h1><p class="lead">${escapeHtml(p.description)}</p><div class="price">${escapeHtml(sold?"売約済み":money(p))}</div>${contactBox}</div></div><div style="margin-top:55px"><div class="eyebrow">仕様</div><h2>主要仕様</h2><div class="spec-table">${rows||"<div class='notice'>仕様はまだ登録されていません。</div>"}</div></div><div style="margin-top:55px"><div class="eyebrow">動画</div><h2>機械動画</h2>${video}</div></div></section>`;}catch{el.innerHTML='<div class="container section"><h2>機械が見つかりません</h2><p class="muted">この掲載は非公開になっている可能性があります。</p><a class="btn" href="/products.html">機械一覧に戻る</a></div>';}}
